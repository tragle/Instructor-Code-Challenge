var Client = Client || function() {

    function log(message) {
        if (window.console && window.console.log) {
            console.log(message);
        }
    }

    log("Client loaded.");

}();


    


